# Agentmap 集成

本项目使用 **agentlens** 进行 AI 优化的文档管理。

## 阅读协议

遵循以下顺序以高效理解代码库：

1. **从这里开始**：`.agentlens/INDEX.md` - 项目概览和模块路由
2. **AI 指令**：`.agentlens/AGENT.md` - 如何使用文档
3. **模块详情**：`.agentlens/modules/{module}/MODULE.md` - 文件列表和入口点
4. **编辑前检查**：查看 `.agentlens/modules/{module}/memory.md` 了解警告/TODO

## 文档结构

```
.agentlens/
├── INDEX.md              # 从这里开始 - 全局路由表
├── AGENT.md              # AI 代理指令
├── modules/
│   └── {module-slug}/
│       ├── MODULE.md     # 模块摘要
│       ├── outline.md    # 大文件的符号映射
│       ├── memory.md     # 警告、TODO、业务规则
│       └── imports.md    # 依赖关系
└── files/                # 复杂文件的深度文档
```

## 开发期间

- 使用 `.agentlens/modules/{module}/outline.md` 查找大文件中的符号
- 检查 `.agentlens/modules/{module}/imports.md` 了解依赖关系
- 对于复杂文件，请参阅 `.agentlens/files/{file-slug}.md`

## 命令

| 任务 | 命令 |
|------|---------|
| 重新生成文档 | `agentlens` |
| 快速更新（仅更改） | `agentlens --diff main` |
| 检查是否过期 | `agentlens --check` |
| 强制完全重新生成 | `agentlens --force` |

## 关键模式

- **模块边界**：`mod.rs` (Rust)、`index.ts` (TS)、`__init__.py` (Python)
- **大文件**：>500 行，有符号大纲
- **复杂文件**：>30 个符号，有 L2 深度文档
- **中心文件**：被 3+ 个文件导入，标记为 🔗
- **内存标记**：TODO、FIXME、WARNING、SAFETY、RULE

---
*由 [agentlens](https://github.com/nguyenphutrong/agentlens) 生成*
